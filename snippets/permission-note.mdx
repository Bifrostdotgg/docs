Bifröst uses its own permission system rather than Discord's built-in role permissions. Access to features is configured through the dashboard under **Permissions**, where you can grant or restrict each capability to specific roles. See the [Permissions guide](/permissions) for a full explanation.
